{
  "name": "dsh-bridge-gateway",
  "version": "0.2.1",
  "description": "以 dsh-bridge 为根基的 DSH 远程访问网关：局域网 / Cloudflare 隧道 / Tailscale 隧道 / 自建隧道 / 公网直连网关 / IM Bot，全部在 Web 面板中配置。",
  "releaseNotes": "【dsh-bridge-gateway 0.2.1】\n• 🐛 修复远程端「改了页面不变」：透明反向代理对 `text/html` 响应强制 `Cache-Control: no-store, must-revalidate` 并去掉 etag/last-modified，普通刷新即可拿到新 index（模块 bundle 的长缓存策略不变）\n• ✅ DSH 0.1.7 兼容性核查：无需改动（宿主 8 个注入服务、客户端槽位、dsh-llm 导出全部仍在），0.1.5 线继续兼容",
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": {
      "default": "./lib/index.js"
    },
    "./client": "./client/client.js",
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "client",
    "cordis.patch.yml",
    "README.md",
    "README.zh.md",
    "CHANGELOG.md",
    "LICENSE",
    "docs"
  ],
  "scripts": {
    "build:client": "node client/build.mjs",
    "build:lark": "node scripts/bundle-lark.mjs",
    "build:banner": "node scripts/generate-banner.mjs",
    "prepack": "npm run build:lark && npm run build:client",
    "release:github": "node scripts/create-github-release.mjs",
    "test": "node --import ./test/isolate-home.mjs --test test/*.test.mjs"
  },
  "dependencies": {
    "qrcode": "^1.5.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@deepseek-ai/dsh-llm": "0.1.0-rc.6",
    "@larksuiteoapi/node-sdk": "^1.73.0",
    "esbuild": "^0.25.9",
    "puppeteer-core": "^25.8.0"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@deepseek-ai/dsh-llm": ">=0.1.0-rc.6"
  },
  "peerDependenciesMeta": {
    "@deepseek-ai/dsh-llm": {
      "optional": true
    }
  },
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "license": "MIT",
  "keywords": [
    "deepseek-harness",
    "dsh",
    "dsh-plugin",
    "remote",
    "remote-access",
    "tunnel",
    "cloudflare",
    "tailscale",
    "qr-code",
    "qrcode",
    "websocket",
    "bot",
    "wechat",
    "qq",
    "feishu",
    "telegram"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "inject": [
        "@deepseek-ai/dsh-client-connection",
        "@deepseek-ai/dsh-client-ui-slots",
        "@deepseek-ai/dsh-client-ui-layout",
        "@deepseek-ai/dsh-client-locale"
      ],
      "platform": "web"
    }
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:lament-z/dsh-bridge-gateway.git"
  },
  "bugs": {
    "url": "https://github.com/lament-z/dsh-bridge-gateway/issues"
  },
  "homepage": "https://github.com/lament-z/dsh-bridge-gateway",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  }
}
